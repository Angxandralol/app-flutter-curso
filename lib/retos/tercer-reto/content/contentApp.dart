import 'package:flutter/material.dart';

class ContentApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final background = Container(
      margin: EdgeInsets.only(top: 449.0),
      height: 500.0,
      color: Colors.black,
    );
    return background;
  }
}
